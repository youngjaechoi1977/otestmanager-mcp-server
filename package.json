{
  "name": "otestmanager-mcp",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "MCP server exposing OTestManager2026 project data (test cases, sessions, results, bugs) to external LLM clients via an account API key (every tool names its project) or a legacy project key.",
  "bin": {
    "otestmanager-mcp": "./index.js"
  },
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  }
}
